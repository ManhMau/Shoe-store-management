{
  "ConnectionStrings": {
    "DefaultConnection": "server=(local);database=ShoeStore;uid=sa;pwd=123;TrustServerCertificate=True;Trusted_Connection=True;"
  }
  //"Jwt": {
  // "Key": "your-secret-key", // Thay bằng một chuỗi bí mật
  //"Issuer": "your-issuer", // Ví dụ: ShoeStoreAPI
  // "Audience": "your-audience", // Ví dụ: ShoeStoreAPIUsers
  // "ExpiresInMinutes": 60 // Thời gian hiệu lực của token (tính bằng phút)
  //}  

}
